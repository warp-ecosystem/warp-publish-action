/**
 * Return a greeting string.
 *
 * @returns {string} The string "World!"
 */
export function hello() {
  return "World!";
}

/**
 * Unused test fixture function.
 *
 * @returns {string} A message indicating this function is unused and should be removed by the compiler
 */
export function unusedFunction() {
  return "This function is unused and should be removed by the compiler.";
}
